# Vehicle,year,month,day,hour,minutes,seconds,time(seconds since Jan 1 1970),latitude(deg),longitude(deg),X local(m),Y local(m),latitude origin(deg),longitude origin(deg),X UTM(m),Y UTM(m),UTMZone,depth(m),altitude(m),octans heading(deg),octans pitch(deg),octans roll(deg),crossbow heading(deg),crossbow pitch(deg),crossbow roll(deg),maggie x,maggie y,maggie z,maggie total,conductivity,temperature(deg C),depth(m),salinity,sound velocity,pressure,lss gain,lss backscatter
#  note:  for all fields in the CSV record, if there is no new data available since the last record, no data appears, but the comma is sent
